---
description: STG integration and workspace tests under testing/
globs: testing/**/*
alwaysApply: false
---

# Testing crate (`testing/`)

- Integration tests expect **SurrealDB + Redis + API** as documented. Start stack with **`./scripts/start-back.sh`** before **`cargo test -p testing`** or use **`./ci-local.sh integration`** / **`./ci-local.sh all`**.
- Prefer env/ports from **`config/`** and **`scripts/load-env.sh`** so tests match CI.
- When assertions involve API contracts, align types with **`shared/`** and **`back/api`** rather than duplicating ad-hoc structs.
