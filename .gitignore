# All generated objects consolidated in _build/
_build/

# Rust — commit Cargo.lock for this application workspace (reproducible CI and local builds).
target/
**/*.rs.bk

# Node — commit lockfiles (package-lock.json, etc.) for reproducible installs.
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Frontend / Trunk build output
dist/

# IDE / editor
.vscode/
.idea/
# Cursor: track shared agent rules; ignore other local IDE state under .cursor/
.cursor/*
!.cursor/rules/
!.cursor/rules/**
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Environment files (keep templates, ignore actual .env files)
.env
.env.*
.env.*.*
!.env.*.template
config/.env.development
config/.env.production
config/.env.*
config/smacktalk.surql
# Expanded environment files (generated from templates)
.env.*.expanded.*

# Docker data
docker-data/

# Logs
*.log
logs/

# Cleanup artifacts
*.old
*.backup
*.bak

# Test results and artifacts
test-results/
playwright-report/
*.lcov
coverage/
*.profraw
deploy/smacktalk.surql
# BGG ranks dump (large; refresh periodically; see data/bgg/)
data/bgg/boardgames_ranks.csv
